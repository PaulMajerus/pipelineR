% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_data.R
\name{format_data}
\alias{format_data}
\title{Reshape Stock Price Data to Long Format}
\usage{
format_data(table = list())
}
\arguments{
\item{table}{A list of \code{tibble}s containing stock price data (typically from \code{tq_get()}).}
}
\value{
A list of reshaped \code{tibble}s with columns including \code{metric} and \code{value}.
}
\description{
Transforms each \code{tibble} in a list from wide format (with columns \code{open}, \code{high}, \code{low}, \code{close})
to long format using \code{pivot_longer()}, resulting in a metric/value structure.
}
\examples{
\dontrun{
data <- yahoo_query_data(symbolsBatches)
formatted <- format_data(data)
}
}
